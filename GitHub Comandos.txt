git init // Crea un nuevo repositorio o inicializa un nuevo repositorio vacio
git status // Muestra el estado del repositorio
git add . // Agrega todos los archivos con el espacio y punto
git commit -M "Se cargo el proyecto" // Guarda todos los cambio hechos en la zona de montaje
git remote add origin https://github.com/Hokaguefire/SEGOB.git // Carga el proyecto en el repositorio
git push -u origin main // Sincroniza el repositorio local con el remoto
git clone https://github.com/Hokaguefire/SEGOB.git // Clona un repositorio
git checkout main // Accede a la rama deseada
git pull origin main // Actualiza la rama
git branch issue#1 // Crea una rama
git branch // Muestra la ubicación de la rama
